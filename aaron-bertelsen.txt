
	Codepen Link:  https://codepen.io/Aaberte/pen/YzPPNZa

    	1. What is Semantic HTML? - The use of HTML to provide meaning to your content
    
	2. What is HTML used for? - Provides meaning to content, makes websites readable and can be intrepreted by machines, screen readers, search engines etc., help keep code organized and easiy to maintain.
		
    	3. What is an attribute and where do we put it? - An attribute provides additional information about an element. You put the attribute in the starting tag.

    	4. What is the h1 tag used for? How many times should I use it on a page? - The h1 tag is used to signify the most important title or heading of a page.  It should only be used once per page.

    	5. Name two tags that have required attributes - <a></a> and <img></img>

    	6. What do we put in the head of our HTML document? - A title tag and a link to a css style sheet.

    	7. What is an id? - Is an attribute that assigns an id to an HTML element.

   	 8. What elements can I add an id to? - All HTML elements.

    	9. How many times can I use the same id on a page? - Only once.

    	10. What is a class? - An attribute used to define equal styles for elements with the same class name.

  	  11. What elements can I add a class to? - Any HTML element.

   	 12. How many times can I use the same class on a page? - As many times as you need to.

   	 13. How do I get my link to open in a new tab? - In the anchor tag after your href attribute where you give your link you use another attribute target="_blank"

   	 14. What is the alt attribute used for? - used in the <img> tag, It gives an alternate text for an image if it cannot be displayed

  	15. How do I reference an id? - you use #id {}

    	16. What is the difference between a section and a div - Section is a generic section of a document that usually has other elements inside.  A div is a generic container that can be used to change styling within a section.

    	17. What is CSS used for? - Adding the style to your content.

    	18. How to we select an element? Example - every h2 on the page - h2 {}

    	19. What is the difference between a class and an id? - Give me an example of when I might use each one - an id can only be used to identify one element on a page, a class can be used to identify multiple elements on a page.  An example would be if you have 


    20. How do we select classes in CSS? with a . so if class="foot" it would be .foot {}

    21. How do we select a p element with a single class of “human””? - p.human {}

    22. What is a parent child selector? When would this be useful? - A parent child selector, selects all elements that are direct children of the parent.  it would be useful in that so you do not have to select each child individually, you can select them all with one section of code.

    23. How do you select all links within a div with the class of sidebar? - sidebar > a {}
    24. What is a pseudo selector? - A keyword added to a selector that specifies a certain state of the selected element.

    25. What do we use the change the spacing between lines? - We use the line-height: property in the CSS for that element.

    26. What do we use to change the spacing between letters? - We use the letter-spacing: property in the CSS for that element.

    27. What do we use to to change everything to CAPITALS? lowercase? Capitalize? - We use the text-transform: property, so for CAPITALS it would be text-transform: uppercase;  Lowercase would be text-transform: lowercase; and Capitalize would be text-transform: capitalize;

    28. How do I add a 1px border around my div that is dotted and black? - In the css for that div I would add   border: 1px dotted black;

    29. How do I select everything on the page? - use the * selector

    30. How do I write a comment in CSS? - /* add your comment in here */

    31. How do I find out what file I am in, when I am using the command line? - type the command pwd and hit enter.

    32. Using the command line - how do I see a list of files/folders in my current folder? - type the ls command and hit enter.

    33. How do I remove a file via the command line? Why do I have to be careful with this? - Use the command rm and then the file name and hit enter.  You have to be careful becuase it is a permantent action.  if you delete a file through the GUI of your computer it will usually go to a recycle bin and/or flash warnings before you permanently delete something.  using the rm command you do not get any warning

    34. Why should I use version control? - Version control helps keep track of all your changes and allows you to revert back if something breaks after an update.

    35. How often should I commit to github? - Whenever you finish a change

    36. What is the command we would use to push our repo up to github? - git push -u origin aaron-bertelsen

    37. Walk me through Lambda's git flow. 
	1. Fork the repo (correct if your name now appears before the project name
	2. Add your TL as a collaborator
	3. Clone your rep
		a. click the clone button to access the URL
		b. copy the URL
		c. use the cd command to go to the directory you want to clone your fork
		d. use git clone <link-to-repo> to clone the repo
	4. cd into the newly created repo using cd <repo-name>
	5.create your own branch, use git checkout -b firstname-lastname
	6. work on your repo in your own branch once you are ready to save:
 		TO Save you need to Add, Commit, Push
	7. git add . (adds the changes and makes ready for commiting)
	8. git commit -m "descriptions of changes"
	9. git push -u origin branch-name

		Now changes are on github you need to submit a PR (Pull Request)
	1.Go back to your forked project on github
		a. click on Branch:master and change to your branch
	2. click on New pull request
	3. click the dropdown on the button that says base repository and change it to be your forked repo
	4. Add your TL to the Reviewers
	5. submit the pull request

